# Server Configuration
PORT=8080

# Base URL for the application (used for SSO redirect URI)
# For local development: https://localhost:8080 (requires HTTPS certificates - see README)
# For deployed environments: Set via Helm chart (e.g., https://cath-web.demo.platform.hmcts.net)
BASE_URL=https://localhost:8080

# Session Configuration
SESSION_SECRET=your-session-secret-key-change-this-in-production

# Redis Configuration
REDIS_URL=redis://localhost:6380

# SSO Configuration (Azure AD)
# SSO secrets are loaded from Azure Key Vault automatically:
# - Local development: pip-ss-kv-stg (requires `az login`)
# - Deployed environments: pip-ss-kv-{ENV} (automatic via managed identity)
#
# Secrets loaded from Key Vault:
# - SSO_IDENTITY_METADATA
# - SSO_CLIENT_ID
# - SSO_CLIENT_SECRET
# - SSO_SYSTEM_ADMIN_GROUP_ID
# - SSO_INTERNAL_ADMIN_CTSC_GROUP_ID
# - SSO_INTERNAL_ADMIN_LOCAL_GROUP_ID

# Allow HTTP redirect for SSO (false for HTTPS, true only if using HTTP)
SSO_ALLOW_HTTP_REDIRECT=false

# Application Insights (Optional)
APPLICATION_INSIGHTS_CONNECTION_STRING=InstrumentationKey=your-instrumentation-key;IngestionEndpoint=https://dc.services.visualstudio.com/

# GOV.UK Notify Configuration
# API key and template IDs for sending notification emails
# Get these from: https://www.notifications.service.gov.uk/
GOVUK_NOTIFY_TEST_API_KEY=your-api-key-here
GOVUK_NOTIFY_TEMPLATE_ID_MEDIA_APPROVAL=template-uuid-here
GOVUK_NOTIFY_TEMPLATE_ID_MEDIA_REJECTION=template-uuid-here
GOVUK_NOTIFY_TEMPLATE_ID_SUBSCRIPTION=template-uuid-here
GOVUK_NOTIFY_TEMPLATE_ID_SUBSCRIPTION_PDF_AND_SUMMARY=template-uuid-here
GOVUK_NOTIFY_TEMPLATE_ID_SUBSCRIPTION_SUMMARY_ONLY=template-uuid-here

# Service URL for generating links in notifications
CATH_SERVICE_URL=https://www.court-tribunal-hearings.service.gov.uk
