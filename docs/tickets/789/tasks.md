# Implementation Tasks

## Implementation Tasks
- [x] In `libs/web-core/src/views/components/cookie-banner.njk`, replace `style="display: none;"` with the `hidden` attribute on the `.cookie-banner-accept-message` div
- [x] In `libs/web-core/src/views/components/cookie-banner.njk`, replace `style="display: none;"` with the `hidden` attribute on the `.cookie-banner-reject-message` div
- [ ] Manually verify in the browser: clicking "Accept analytics cookies" shows the accepted confirmation panel and hides the initial prompt
- [ ] Manually verify in the browser: clicking "Reject analytics cookies" shows the rejected confirmation panel and hides the initial prompt
- [ ] Manually verify in the browser: clicking "Hide cookie message" removes the banner entirely
