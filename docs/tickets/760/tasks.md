## Implementation Tasks

- [x] Rename sub-jurisdiction id 24 in `libs/location/src/location-data.ts` — `name` → "First-tier Tribunal (Property Chamber) - Residential Property Division", `welshName` → "Tribiwnlys Haen Gyntaf (Siambr Eiddo) – Adran Eiddo Preswyl"
- [x] Check `libs/location/src/seed-data.test.ts` and `libs/location/src/seed-list-types.test.ts` for hard-coded assertions on the old sub-jurisdiction 24 name; update if present (checked — both use synthetic mock fixtures, no reference to real location-data.ts values, no changes needed)
- [x] Add `FTT_RPT_MARKET_RENTS_WEEKLY_HEARING_LIST` entry to `libs/list-types/common/src/list-type-data.ts` (englishFriendlyName, welshFriendlyName, provenance `CFT_IDAM`, urlPath `ftt-rpt-weekly-hearing-list`, isNonStrategic `true`, defaultSensitivity `Public`, shortenedFriendlyName "FTT (RPT): Market Rents Weekly Hearing List", subJurisdictionIds `[24]`)
- [x] Check `apps/postgres/prisma/generate-seed-sql.test.ts` for any snapshot/count assertion on `listTypeData`/`locationData` length; update if present (checked — uses synthetic fixtures, no dependency on real data length, no changes needed)
- [x] Update `libs/list-types/ftt-rpt-weekly-hearing-list/src/locales/en.ts`: replace static `importantInformationText` with `importantInformationTextTemplate` (email placeholder) — **retain existing "telephone or video" wording per resolved Clarification 2**, do not drop "telephone" — + keep/adjust second paragraph; add `rptRegionalEmail` map keyed by list type name using **exact ticket casing** per resolved Clarification 4 (`RPEastern@justice.gov.uk`, `rpmidland@justice.gov.uk`, `rpnorthern@justice.gov.uk`, `RPSouthern@justice.gov.uk`, `London.Rap@justice.gov.uk`, `marketrents@justice.gov.uk` — no normalisation); add `marketRentsExtraInformation`; add `rptMarketRentsCourtName` and `rptMarketRentsPageTitle`
- [x] Update `libs/list-types/ftt-rpt-weekly-hearing-list/src/locales/cy.ts` — **expanded scope per resolved Clarification 6**: this file is currently byte-identical to `en.ts` (English text under every Welsh key) and the *entire* file must be corrected, not just new/changed keys. Follow the per-key sourcing table in `plan.md` §3a:
  - [x] Replace with verified genuine Welsh (reused verbatim from `pht-weekly-hearing-list`/`care-standards-tribunal-weekly-hearing-list` cy.ts, already confirmed identical boilerplate elsewhere): `listForWeekCommencing`, `lastUpdated`, `at`, `factLinkText`, `factAdditionalText`, `importantInformationTitle`, `importantInformationLinkText`, `dataSource`, `backToTop`, `cautionNote`, `cautionReporting`, `searchCasesTitle`, `tableHeaders.additionalInformation`
  - [x] Insert ticket-supplied Welsh (new mechanism, see plan.md §2/§3a) for: `importantInformationTextTemplate` (with `{email}` placeholder), `importantInformationSecondParagraph`, `marketRentsExtraInformation`, `rptMarketRentsCourtName`, `rptMarketRentsPageTitle`
  - [x] Apply the `[WELSH TRANSLATION REQUIRED: '<English value>']` placeholder convention (precedent: `grc-weekly-hearing-list/src/locales/cy.ts`) for keys with **no verified translation anywhere in the codebase**: `searchCasesLabel`, `tableHeaders.date`, `tableHeaders.time`, `tableHeaders.venue`, `tableHeaders.caseType`, `tableHeaders.caseReferenceNumber`, `tableHeaders.judges`, `tableHeaders.members`, `tableHeaders.hearingMethod`, and the 5 existing `rpt{Eastern,London,Midlands,Northern,Southern}CourtName`/`PageTitle` keys (untouched per Clarification 1, but still lacking a real translation today)
  - [x] Do not invent legal/procedural Welsh — only reuse strings verified as identically translated elsewhere; everything else must use the placeholder pending sign-off from Kimberley Newton (named Welsh translator in the ticket)
- [x] Add a small helper (e.g. `buildImportantInformationText(template, email)`) to substitute `{email}` into the template string — colocate with the locale files or in `rendering/renderer.ts` (added `libs/list-types/ftt-rpt-weekly-hearing-list/src/locales/important-information-text.ts`, exported from `index.ts`, with co-located test)
- [x] Register the Market Rents Excel converter: add `registerConverterByName("FTT_RPT_MARKET_RENTS_WEEKLY_HEARING_LIST", fttRptConverter)` in `libs/list-types/ftt-rpt-weekly-hearing-list/src/conversion/ftt-rpt-config.ts`
- [x] Add `FTT_RPT_MARKET_RENTS_WEEKLY_HEARING_LIST` to `PDF_GENERATOR_REGISTRY` in `libs/publication/src/processing/service.ts` (courtName "First-tier Tribunal (Residential Property Tribunal)", listTitle "First-tier Tribunal (Residential Property Tribunal): Market Rents Weekly Hearing List")
- [x] Add `FTT_RPT_MARKET_RENTS_WEEKLY_HEARING_LIST` to the email-builder registry in `libs/notifications/src/notification/notification-service.ts` (reuse `extractFttRptSummary` / `formatFttRptSummaryForEmail`)
- [x] Update `apps/web/src/pages/(list-types)/ftt-rpt-weekly-hearing-list/index.ts`: add Market Rents to `LIST_TYPE_CONFIG`; resolve `importantInformationText` (template + regional email lookup by `listTypeName`); resolve `extraInformationText` (Market Rents only, else empty string); pass both to `res.render`
- [x] Update `apps/web/src/pages/(list-types)/ftt-rpt-weekly-hearing-list/ftt-rpt-weekly-hearing-list.njk`: render `{{ importantInformationText }}` (controller-resolved) instead of the locale-key string; add conditional bold paragraph for `extraInformationText`
- [x] Delete stale duplicate template `libs/list-types/ftt-rpt-weekly-hearing-list/src/views/ftt-rpt-weekly-hearing-list.njk` (confirmed unused/dead — see plan.md §1; verified via grep no code references before deleting)
- [x] Add a unit test asserting every `LIST_TYPE_CONFIG` key has a matching `rptRegionalEmail` entry in both `en` and `cy` (guards against a future missing-email regression) (added `libs/list-types/ftt-rpt-weekly-hearing-list/src/locales/regional-email-guard.test.ts`, driven off `listTypeData` filtered by `urlPath === "ftt-rpt-weekly-hearing-list"` — the real source of truth — rather than a hand-duplicated list of names)
- [x] Update `apps/web/src/pages/(list-types)/ftt-rpt-weekly-hearing-list/index.test.ts`: add a 6th region case for Market Rents; assert resolved email per region; assert `extraInformationText` only present for Market Rents
- [x] Update `apps/web/src/pages/(list-types)/ftt-rpt-weekly-hearing-list/ftt-rpt-weekly-hearing-list.njk.test.ts`: assert substituted email renders (and the literal `[insert office email]` placeholder never renders); assert bold paragraph renders only when `extraInformationText` is passed (these assertions already existed from a prior session; fixed an unescaped `!` in the `govuk-!-font-weight-bold` Cheerio selector that was silently matching 0 elements — CSS requires `\\!` to select GOV.UK's `!` utility classes)
- [x] Add/extend a PDF generator test case for `FTT_RPT_MARKET_RENTS_WEEKLY_HEARING_LIST` in `libs/list-types/ftt-rpt-weekly-hearing-list/src/pdf/pdf-generator.test.ts`
- [x] Confirm locale-key parity test (`Object.keys(en).sort()).toEqual(Object.keys(cy).sort())`) still passes with all new keys (verified via `yarn vitest run libs/list-types/ftt-rpt-weekly-hearing-list` — 7 test files, 41 tests, all passed, including the "Locale consistency" describe block in `ftt-rpt-weekly-hearing-list.njk.test.ts`)
- [x] Run `yarn db:seed` locally to verify the sub-jurisdiction rename and new list type land correctly and link to sub-jurisdiction 24 (ran against local docker postgres — `sub_jurisdiction.name`/`welsh_name` for id 24 confirmed updated; `list_types` row `FTT_RPT_MARKET_RENTS_WEEKLY_HEARING_LIST` (id 315) confirmed seeded with `url = ftt-rpt-weekly-hearing-list`, `is_non_strategic = t`, `shortened_friendly_name = "FTT (RPT): Market Rents Weekly Hearing List"`; `list_types_sub_jurisdictions` junction row confirms link to `sub_jurisdiction_id = 24`)
- [x] Verify `/non-strategic-upload` dropdown displays "FTT (RPT): Market Rents Weekly Hearing List" locally (the dropdown options come from `findNonStrategicListTypes()` in `libs/system-admin-pages/src/list-type/queries.ts` — `WHERE is_non_strategic = true AND deleted_at IS NULL ORDER BY shortened_friendly_name ASC` — ran that exact query against the local seeded DB and confirmed row `id 315, shortened_friendly_name = "FTT (RPT): Market Rents Weekly Hearing List"` is present and correctly ordered alongside the other FTT (RPT) region entries)
- [x] Add one Playwright `@nightly` publisher-journey test and one public-viewer-journey test per plan.md §5 (accessibility scan inline, Welsh check inline, no separate per-AC tests) (added `e2e-tests/tests/ftt-rpt-market-rents.spec.ts` following the exact precedent in `summary-of-publications.spec.ts` and `courts-tribunals-list.spec.ts`; both tests tagged `@nightly` and statically validated via `npx biome check --write` and `npx playwright test --list` — confirmed the file parses and both tests register correctly. **Could not execute a live run**: `e2e-tests/run-with-credentials.js` requires fetching SSO/session secrets from the real Azure Key Vault `cath-bootstrap-stg-kv` via `DefaultAzureCredential`, which is unavailable in this sandbox — the app cannot boot without `SESSION_SECRET`/`SSO_CLIENT_SECRET`/etc. This is an environment limitation, not a defect; a live run against a real environment with Key Vault access is needed before merge to catch any selector/timing issues)
- [x] Run `yarn lint:fix` and `yarn test` across affected workspaces before opening the PR (`yarn lint:fix`: 65/65 workspaces clean, no new fixes beyond files already touched by this ticket; `yarn test`: initial run showed 64/65 passing with `@hmcts/web` failing on an unrelated `EADDRINUSE :::8080` — traced to a stray leftover process (PID 48871) holding port 8080 from earlier local dev-server experimentation, unrelated to any file this ticket touches (`server.ts`/`server.test.ts` have zero diff). Killed the stray process and re-ran `@hmcts/web` in isolation: 354 test files, 3656 tests passed, 3 skipped, 0 failures)
