{% from "govuk/components/service-navigation/macro.njk" import govukServiceNavigation %}

{% set navigationItems = [] %}
{% if navigation and navigation.verifiedItems %}
  {% for item in navigation.verifiedItems %}
    {% set navigationItems = (navigationItems.push(item), navigationItems) %}
  {% endfor %}
{% endif %}

{% set navigationEndHtml %}
  {% if isAuthenticated %}
    <li class="govuk-service-navigation__item"><a class="govuk-service-navigation__link" href="/logout">{{ navigation.signOut if (navigation and navigation.signOut) else "Sign out" }}</a></li>
  {% else %}
    <li class="govuk-service-navigation__item"><a class="govuk-service-navigation__link" href="/sign-in">{{ navigation.signIn if (navigation and navigation.signIn) else "Sign in" }}</a></li>
  {% endif %}
{% endset %}

{{ govukServiceNavigation({
  serviceName: serviceName,
  serviceUrl: "/",
  navigation: navigationItems if navigationItems.length > 0 else undefined,
  slots: {
    navigationEnd: navigationEndHtml
  },
  rebrand: true
}) }}
