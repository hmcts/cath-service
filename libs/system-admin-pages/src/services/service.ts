export async function sendPublicationNotifications(artefactId: string): Promise<void> {
  // Mock implementation - placeholder for future notification service integration
  console.log(`Mock: Sending notifications for artefact ${artefactId}`);
}
