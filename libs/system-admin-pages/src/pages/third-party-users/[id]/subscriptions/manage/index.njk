{% extends "layouts/base-template.njk" %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}

{% block backLink %}
  <a href="/third-party-users/{{ userId }}/manage{{ lngParam }}" class="govuk-back-link">{{ back }}</a>
{% endblock %}

{% block content %}
<div class="govuk-grid-row">
  <div class="govuk-grid-column-full">
    <h1 class="govuk-heading-l">{{ pageTitle }}</h1>
    <p class="govuk-body govuk-!-margin-bottom-4">{{ pageOf }}</p>

    <form method="post" novalidate>

      {% if useDropdown %}
        <table class="govuk-table">
          <thead class="govuk-table__head">
            <tr class="govuk-table__row">
              <th scope="col" class="govuk-table__header">{{ tableHeadings.listType }}</th>
              <th scope="col" class="govuk-table__header">{{ tableHeadings.sensitivity }}</th>
            </tr>
          </thead>
          <tbody class="govuk-table__body">
            {% for listType in listTypes %}
              {% set currentValue = currentSubscriptions[listType.name] or "UNSELECTED" %}
              <tr class="govuk-table__row">
                <td class="govuk-table__cell">{{ listType.englishFriendlyName }}</td>
                <td class="govuk-table__cell">
                  <select class="govuk-select" id="{{ listType.name }}" name="{{ listType.name }}" aria-label="{{ tableHeadings.sensitivity }} for {{ listType.englishFriendlyName }}">
                    <option value="UNSELECTED" {{ "selected" if currentValue == "UNSELECTED" else "" }}>{{ sensitivityOptions.unselected }}</option>
                    <option value="PUBLIC" {{ "selected" if currentValue == "PUBLIC" else "" }}>{{ sensitivityOptions.public }}</option>
                    <option value="PRIVATE" {{ "selected" if currentValue == "PRIVATE" else "" }}>{{ sensitivityOptions.private }}</option>
                    <option value="CLASSIFIED" {{ "selected" if currentValue == "CLASSIFIED" else "" }}>{{ sensitivityOptions.classified }}</option>
                  </select>
                </td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      {% else %}
        <table class="govuk-table">
          <thead class="govuk-table__head">
            <tr class="govuk-table__row">
              <th scope="col" class="govuk-table__header">{{ tableHeadings.listType }}</th>
              <th scope="col" class="govuk-table__header">{{ tableHeadings.public }}</th>
              <th scope="col" class="govuk-table__header">{{ tableHeadings.private }}</th>
              <th scope="col" class="govuk-table__header">{{ tableHeadings.classified }}</th>
              <th scope="col" class="govuk-table__header">{{ tableHeadings.unselected }}</th>
            </tr>
          </thead>
          <tbody class="govuk-table__body">
            {% for listType in listTypes %}
              {% set currentValue = currentSubscriptions[listType.name] or "UNSELECTED" %}
              <tr class="govuk-table__row">
                <td class="govuk-table__cell">{{ listType.englishFriendlyName }}</td>
                <td class="govuk-table__cell">
                  <div class="govuk-radios__item">
                    <input class="govuk-radios__input" id="{{ listType.name }}-public" name="{{ listType.name }}" type="radio" value="PUBLIC" {{ "checked" if currentValue == "PUBLIC" else "" }}>
                    <label class="govuk-label govuk-radios__label" for="{{ listType.name }}-public"><span class="govuk-visually-hidden">{{ sensitivityOptions.public }} for {{ listType.englishFriendlyName }}</span></label>
                  </div>
                </td>
                <td class="govuk-table__cell">
                  <div class="govuk-radios__item">
                    <input class="govuk-radios__input" id="{{ listType.name }}-private" name="{{ listType.name }}" type="radio" value="PRIVATE" {{ "checked" if currentValue == "PRIVATE" else "" }}>
                    <label class="govuk-label govuk-radios__label" for="{{ listType.name }}-private"><span class="govuk-visually-hidden">{{ sensitivityOptions.private }} for {{ listType.englishFriendlyName }}</span></label>
                  </div>
                </td>
                <td class="govuk-table__cell">
                  <div class="govuk-radios__item">
                    <input class="govuk-radios__input" id="{{ listType.name }}-classified" name="{{ listType.name }}" type="radio" value="CLASSIFIED" {{ "checked" if currentValue == "CLASSIFIED" else "" }}>
                    <label class="govuk-label govuk-radios__label" for="{{ listType.name }}-classified"><span class="govuk-visually-hidden">{{ sensitivityOptions.classified }} for {{ listType.englishFriendlyName }}</span></label>
                  </div>
                </td>
                <td class="govuk-table__cell">
                  <div class="govuk-radios__item">
                    <input class="govuk-radios__input" id="{{ listType.name }}-unselected" name="{{ listType.name }}" type="radio" value="UNSELECTED" {{ "checked" if currentValue == "UNSELECTED" else "" }}>
                    <label class="govuk-label govuk-radios__label" for="{{ listType.name }}-unselected"><span class="govuk-visually-hidden">{{ sensitivityOptions.unselected }} for {{ listType.englishFriendlyName }}</span></label>
                  </div>
                </td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      {% endif %}

      {% if isLastPage %}
        {{ govukButton({ text: saveButton }) }}
      {% else %}
        {{ govukButton({ text: nextButton }) }}
      {% endif %}

    </form>

  </div>
</div>
{% endblock %}
