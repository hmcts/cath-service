# Plan — Issue #<number>: <title>

## 1. Technical Approach

[2-4 paragraphs describing what needs to be built and how. Explain the approach in plain terms before listing files. Note which existing implementation this follows as a pattern.]

## 2. Implementation Details

### 2.1 Pattern to follow

[Name the existing file/module that this work most closely resembles. E.g. "Follow the pattern in `libs/list-types/grc-weekly-hearing-list/`".]

### 2.2 Files to create

| File | Purpose |
|---|---|
| `path/to/file.ts` | [What it does] |

### 2.3 Files to modify

| File | Change |
|---|---|
| `path/to/file.ts` | [What changes and why] |

### 2.4 Key constraints

- [Constraint 1: e.g. "Must use `listTypeName` not `listTypeId` — see CLAUDE.md"]
- [Constraint 2: e.g. "Schema file must have a corresponding validator export — CI guard will fail otherwise"]

## 3. Sequencing

[Note any dependencies between tasks: what must be done before something else can start.]
